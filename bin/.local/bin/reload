#!/bin/bash

########################
# Reloads arch components. Can be triggerd with SUPER + R
########################

killall dunst ; hyprpanel -q; hyprpanel & disown

swww restore

~/.dotfiles/stow.sh
