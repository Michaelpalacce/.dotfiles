#!/usr/bin/env bash

git config --global user.name "Stefan Genov"
git config --global user.email "sgenov94@gmail.com"
git config --global core.editor "nvim"
git config --global --add --bool push.autoSetupRemote true
